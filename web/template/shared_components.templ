package template

templ skeleton(apiRoute string) {
	<p class={ cardSkeleton() } aria-busy="true" hx-get={ apiRoute } hx-trigger="load delay:1s" hx-swap="outerHTML transition:true">
		<strong>fetching data...</strong>
	</p>
}

templ ErrorFetchCard() {
	<article class={ cardSkeleton() }>
		<strong>Uh oh! failed to fetch data.</strong>
	</article>
}
